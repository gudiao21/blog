<h1>Articles#index</h1>
<p>Find me in app/views/articles/index.html.erb</p>
<h1>Hello, Rails!</h1>
<hr>
<br>
<ul>
    <%#Se eu colocar '<%= @articles.each do |article|', imprimir'um por um. %>
    <% @articles.each do |article| %>
        <li>
            <!-- <a href="/articles/<%= article.id %>"> -->
            <!--<a href="<%= article_path(article) %>"> -->
                <%= link_to article.title, article %> <!--Mostra em formato de link: 'Hello Rails', que me vai levar para o link "/articles/<%= article.id %>"-->
        </li>
    <% end %>
</ul>

<%= link_to "New Article", new_article_path %>